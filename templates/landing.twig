{% extends 'base.twig' %}

{% block content %}
<section style="position:relative; overflow:hidden; background:linear-gradient(135deg, var(--bg-card) 0%, var(--bg-dark) 100%);">
  <div style="position:absolute; top:-100px; right:-100px; width:400px; height:400px; border-radius:50%; background:radial-gradient(circle, color-mix(in srgb, var(--secondary) 20%, transparent), transparent); filter:blur(40px);"></div>
  <div style="position:absolute; bottom:10%; left:-50px; width:300px; height:300px; border-radius:50%; background:radial-gradient(circle, color-mix(in srgb, var(--primary) 20%, transparent), transparent); filter:blur(40px);"></div>

  <div style="max-width:1440px; margin:0 auto; padding:8rem 2rem 12rem; position:relative;">
    <div style="text-align:center; max-width:800px; margin:0 auto;">
      <h1 style="font-size:clamp(2.5rem, 6vw, 4.5rem); font-weight:800; margin-bottom:1rem;">Manage Tickets Like a Pro</h1>
      <p style="color:var(--text-secondary); font-size:1.125rem; margin-bottom:2rem;">Streamline your workflow with our powerful ticket management system.</p>

      <div style="display:flex; gap:1rem; justify-content:center; flex-wrap:wrap;">
        <a href="/auth/signup" style="padding:1rem 2.5rem; background:var(--primary-grad); color:var(--text-primary); border:1px solid var(--border); border-radius:12px; font-weight:700; text-decoration:none;">Get Started Free</a>
        <a href="/auth/login" style="padding:1rem 2.5rem; background:var(--glass); color:var(--text-primary); border:2px solid var(--border); border-radius:12px; font-weight:700; text-decoration:none;">Sign In</a>
      </div>
    </div>
  </div>

  <img src="/assets/wave.svg" alt="" style="position:absolute; bottom:0; left:0; width:100%; height:auto;"/>
</section>

<section style="background:var(--bg-dark); padding:6rem 2rem;">
  <div style="max-width:1440px; margin:0 auto;">
    <h2 style="text-align:center; font-size:clamp(2rem,4vw,3rem); margin-bottom:3rem;">Everything You Need</h2>
    <div style="display:grid; grid-template-columns:repeat(auto-fit,minmax(280px,1fr)); gap:1.5rem;">
      {% for f in [
        {icon:'🎯',title:'Smart Organization',desc:'Organize tickets by status, priority, and category'},
        {icon:'⚡',title:'Lightning Fast',desc:'Create, update, and manage tickets in milliseconds'},
        {icon:'🔒',title:'Secure & Private',desc:'Data protected with modern best practices'}
      ] %}
        <div style="background:var(--bg-card); border:1px solid var(--border); border-radius:20px; padding:2rem; box-shadow:0 8px 24px var(--shadow);">
          <div style="font-size:2rem;">{{ f.icon }}</div>
          <h3 style="margin:.5rem 0 0.25rem;">{{ f.title }}</h3>
          <p style="color:var(--text-secondary)">{{ f.desc }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>
{% endblock %}
